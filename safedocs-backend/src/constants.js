const DOCUMENT_CATEGORIES = ['academic', 'research', 'project', 'other'];
const VALID_ROLES = ['student', 'professor', 'admin', 'super_admin'];

module.exports = {
  DOCUMENT_CATEGORIES,
  VALID_ROLES
};
